{
    "tasks": [
        {
            "type": "shell",
            "label": "gcc build active file",
            "command": "/home/julien/.nix-profile/bin/gcc",
            "args": [
                "-g",
                "${file}",
                "-o",
                "${fileDirname}/${fileBasenameNoExtension}"
            ],
            "options": {
                "cwd": "/home/julien/.nix-profile/bin"
            }
        }
    ],
    "version": "2.0.0"
}